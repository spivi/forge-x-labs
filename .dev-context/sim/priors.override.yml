# Fitted priors override -- generated by `lemmings fit` from real KPIs.
# Deep-merged on top of priors_default.yml. Safe to regenerate.
agents:
  developer:
    cost_rate_per_min: 0.1095
bugs:
  base_lambda: 0.0
