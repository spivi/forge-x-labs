"""``checkov_policy_index`` adapter (STUB — implemented in ticket #7).

METADATA-ONLY extraction from a recorded fixture HTML of the Checkov Terraform policy
index: policy IDs, resource types, titles, severities. **Never copies rule source or
logic** (``reuse_status: metadata_only``). Confidence default 0.55; NOT training-eligible
by default. No live Checkov/OPA, no internet. No extraction logic lives here in the
foundation ticket.

See design §8 and §3 (Tier 1).
"""

from __future__ import annotations

# Implemented in ticket #7 (Implement Checkov policy index adapter, metadata-only).
