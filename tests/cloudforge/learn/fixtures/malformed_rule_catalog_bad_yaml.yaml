# Fixture: intentionally malformed YAML (unbalanced brackets) to test the adapter's
# clear-error-not-crash path.
entries:
  - id: broken
    title: "Missing bracket close"
    domains: [storage, data
